.row
  .col-md-12.filtered-somne
    .input-group.filtered-professors
      .input-group-btn.filtered-professors
        button.btn.btn-default.dropdown-toggle.filtered-professors(type='button', data-toggle='dropdown', aria-haspopup='true', aria-expanded='false')
            | Select a Department

        .dropdown-menu
            li(*ngFor='let department of departments')
              a.dropdown-item((click)='selectItem(department)') {{department}}

