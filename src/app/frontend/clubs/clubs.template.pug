navbar
header(headerName='clubs')
.clubs
  .container
    .row
      .col-xs-10.col-centered
        .row
          .col-xs-10.col-sm-6.col-md-4.col-lg-3.club-style(*ngFor='let club of listing.collection' routerLink='../club/{{club.id}}')
            .card.card-inverse
              img.card-img.img-fluid(src='https://www.aubg.edu/upload/event/group-1/709/AUBG%20Olympics.jpg', alt='{{club.name}}')
              //img.card-img.img-fluid(src='{{club.logo}}', alt='{{club.name}}')

              .card-img-overlay
                h4.card-title {{club.name | uppercase}}


home-footer
