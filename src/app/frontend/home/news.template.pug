.regular-news
  .container
    .card-columns
        .card(*ngFor='let post of posts')
          img.card-img-top(src='{{post.image}}', alt='Card image cap')

          .card-block
            h4.card-title {{post.title}}

            p.card-text {{post.created_at | date:"fullDate" }}

            a.card-link(routerLink='/post/{{post.id}}') Read More


